{
  "name": "jenkins-backend-starter",
  "version": "1.0.0",
  "private": true,
  "description": "A small Docker-ready backend for Jenkins CI/CD",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}